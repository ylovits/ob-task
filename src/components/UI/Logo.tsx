import React from "react";
import logo from "../../assets/logo.svg";

const Logo = () => {
  return (
    <div className="ob-logo clickable">
      <img src={logo} alt="logo" />
    </div>
  );
};

export default Logo;
